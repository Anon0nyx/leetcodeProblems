def opposite_ends(arr: list) -> int:
	left = 0
	right = len(arr) - 1

	ans = 0

	while left < right:
		# Perform some logic to solve a problem
		
		if CONDITION: # This is where we put our condition
			left += 1
		else:
			right += 1
		
	return ans
